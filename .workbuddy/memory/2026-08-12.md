# 2026-08-12

新建万年历 Android App 项目（C:\GitHub\WanNianLi）。
- 使用 Kotlin + Jetpack Compose + AGP 8.11.1 + Gradle 8.13 + compileSdk 36。
- 实现了农历/公历转换、天干地支、生肖、节气、星座、宜忌、彭祖百忌、胎神占方、纳音五行、二十八宿、六曜、十二建神、伊斯兰历、佛历年等算法。
- 完成了主界面（年月选择、日历网格、选中高亮）与详情界面（干支、农历、宜忌、各类术数信息）。
- 沙箱环境无法完成 Gradle 构建（native-platform.dll 加载/文件权限问题），源码可直接在 Android Studio 中打开构建。

## 发布 APK（11:31 完成）
- 实际在**本机（非沙箱）**用 `gradlew.bat assembleDebug/assembleRelease` 成功构建。关键点：必须 `rm -rf` 清除 `GRADLE_USER_HOME/native` 目录后再构建，否则 native-platform.dll 的 .lock 文件会 AccessDenied。
- 修复了两处 Kotlin 编译错误（LunarCalendar.kt 291/308 行：Long 索引需 .toInt()）。
- 生成发布密钥库：`keystore/release-key.jks`（别名 wannianli，密码 wannianli2026，已 gitignore）。签名凭据迁移到 `local.properties`（已 gitignore），`app/build.gradle` 读取，避免密码入库。
- 产物：`app/build/outputs/apk/release/app-release.apk`（约 10.6MB），apksigner 校验 v2 签名有效。
- 友好副本：`WanNianLi-release-v1.0.apk`。
- 后续若换机器构建：需拷贝 keystore + local.properties 的 RELEASE_* 项；**丢失 keystore 将无法更新已上架应用**。
